# YouTube Factory — 프로젝트 규칙

> 이 저장소는 영상 1개를 만드는 곳이 아니라, **지식을 영상자산으로 변환하는 생산설비(자본재)**다.
> 모든 작업의 단위 목표는 "영상 1개"가 아니라 "재사용 가능한 모드 1개"다.

## 파이프라인 개요

```
topic-scout → researcher → scriptwriter ⇄ script-critic → video-producer → thumbnail-meta → upload-cron
                                                              ↘ 성과 데이터 → 모드 저장/개선 (modes/)
```

- 에이전트 정의: `.claude/agents/`
- 전략 스킬(서적 코드화): `.claude/skills/`
- 결정론적 도구: `scripts/` (LLM이 아닌 코드로 처리)
- 검증된 모드 스냅샷: `modes/`
- 사실 리소스 풀: `sources/`
- 산출물(누적 자산): `library/`

## LLM vs 코드 경계 (반드시 준수)

| 코드로 처리 (scripts/) | LLM으로 처리 (agents/) |
|---|---|
| 자막 추출(yt-dlp), 아웃라이어 계산, 업로드 API, 파일 배치, cron | 주제 큐레이션, 에버그린·소스 판단, 대본 작성·비평, 썸네일 카피 |

결정론적으로 처리 가능한 작업을 LLM 판단에 맡기지 않는다. 반대로, 판단·생성 작업을 휴리스틱 코드로 때우지 않는다.

## 저작권 가드레일 (위반 시 발행 금지)

1. **사실(fact)·퍼블릭 도메인·공공자료·정당한 인용**만 소스로 사용한다.
2. yt-dlp로 수집한 자막은 **리서치 인풋으로만** 사용한다. 원문 문장 복제·번역 전재 금지. 반드시 독창적으로 재구성한다.
3. 이미지·BGM은 라이선스 확인 후 사용하고, 출처를 `library/` 산출물 메타에 기록한다.
4. 사회·경제(프리미엄 버티컬) 콘텐츠는 **공개 출처만 사용**하고, **개인·기업 실명 지적(비난) 금지**.

## AI 슬롭 차단 규칙

1. **사실 소스 강제**: 대본의 모든 주장은 `sources/`의 출처 태그 달린 리소스에 근거해야 한다. 무근거 생성 금지.
2. **출처 추적**: 대본 내 주요 주장 ↔ 소스 ID 매핑을 유지한다 (`library/scripts/*/claims.md`).
3. **비평 게이트**: 모든 대본은 `script-critic`의 점수화 평가를 통과해야 제작 단계로 넘어간다.
4. **프롬프트 역공정**: AI 특유의 문체(과도한 나열, 기계적 전환어, 공허한 일반론)를 제거하고 전문 작가 톤으로 복원한다.

## 대본 톤 규칙 (시니어 친화 레이어)

주제는 전 연령 에버그린 지식. **전달 방식만** 시니어가 좋아하도록:

- 천천히, 또박또박 읽히는 문장 길이 (한 문장 1.5호흡 이내)
- 어려운 용어는 등장 즉시 한 줄로 풀이
- 회상·향수 자극 (역사적 사건은 "그 시절" 감각과 연결)
- 큰 그림과 의미 부여로 마무리
- 시청자를 존중하는 톤 (가르치려 들지 않기, 호들갑 금지)

상세: `.claude/skills/senior-friendly/SKILL.md`

## 주제 선정 규칙

- **아웃라이어 점수 = 조회수 ÷ 채널 중앙값(최근 20~50개)**. 3배=분석가치, 8배=심층연구, 25배=돌파.
- 주제형 + 에버그린만 채택. 속도형(트렌드) 제외.
- 1차 필터: "양질의 고전·학술·공공 자료가 존재하는가?" 소스가 빈약한 주제는 점수가 높아도 버린다.

## 모드 저장 규칙 (자본재 축적의 단위)

주제→대본→영상→발행까지 끝까지 잘 작동한 설정은 반드시 `modes/`에 스냅샷으로 저장한다.
형식은 `modes/_template.yaml` 참조. 모드 없이 끝난 성공 사례는 자본재로 남지 않는다.

## 언어

- 작업 언어·산출물 기본 언어: 한국어. 영어 채널용 산출물은 `library/renders/en/`.
- 용어 일관성: `config/glossary.md`를 따른다.
